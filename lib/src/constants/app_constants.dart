class AppConstants {
  static const String emailRegex = r"^[\w-\.]+@([\w-]+\.)+[\w]{2,4}$";
  static const String userCollection = "users";
  static const String movieCollection = "movies";
  static const String tokenCollection = "tokens";
}
